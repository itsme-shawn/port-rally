# docker - DEV 실행용 설정
# 사용법: docker-compose에서 .env.dev를 로드하여 실행

# Reactor 디버그 에이전트 비활성화
# spring:
#   reactor:
#     debug-agent:
#       enabled: false

# R2DBC SQL 쿼리 로깅 (개발용)
# logging:
#   level:
#     io.r2dbc.postgresql.QUERY: DEBUG
#     io.r2dbc.postgresql.PARAM: DEBUG
#     org.springframework.r2dbc: DEBUG
#     org.springframework.data.r2dbc: DEBUG

