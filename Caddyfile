{
    # Caddy가 로컬 환경을 위한 인증서를 자동으로 생성하고 신뢰하도록 설정합니다.
    local_certs
}

# Web App (Next.js)
dev.port-rally.com {
    reverse_proxy localhost:3000
}

# Core API (Spring Boot)
dev.api.port-rally.com {
    reverse_proxy localhost:8080
}